# This is a Databricks asset bundle definition for youtube_project_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: youtube_project_bundle
  uuid: 0bada1ca-c6e9-496d-b35d-16b06ed2bde2

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use
  warehouse_id:
    description: WarehouseID

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-85f2e169-6a0a.cloud.databricks.com
    variables:
      catalog: youtube_dev
      schema: dev
      warehouse_id: 3d4310855c1660d7
    presets:
      artifacts_dynamic_version: true
  prod:
    mode: production
    workspace:
      host: https://dbc-85f2e169-6a0a.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/aravalokesh831@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/aravalokesh831@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: youtube_prod
      schema: prod
      warehouse_id: 3d4310855c1660d7
    permissions:
      - user_name: aravalokesh831@gmail.com
        level: CAN_MANAGE
